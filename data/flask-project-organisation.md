# Flask Project Organisation

Create project root directory:

```
mkdir nameproject
cd nameproject
```

The project directory contain:

* nameapp/ : Python package (corresponding to an app) containing your app code and files
* tests/
